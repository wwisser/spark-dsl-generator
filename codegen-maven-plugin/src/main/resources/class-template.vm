package io.sparkdsl.codegen.schema;

import org.apache.spark.sql.Column;
import static org.apache.spark.sql.functions.col;

/**
 * Class generated by Spark DSL.
 * For issues or pull requests check the <a href="https://github.com/wwisser/spark-dsl-generator">repo</a>.
 */
public final class ${className} {

#foreach($field in $fields)
    public static final Column ${field.toUpperCase()} = col("${field}");
#end

    private ${className}() {}

}
